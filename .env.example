# ---------------------------------------------------------------------------
# Frontend environment (Vite). Only PUBLIC values belong here.
# NEVER put an App Secret or an access token in this file.
# ---------------------------------------------------------------------------

VITE_SUPABASE_URL=https://exoajfewjydcgxsslswq.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_WLOTZrLh22JQ_qMxJQ-UKw_-H4Fe17m

# Instagram OAuth redirect URI.
# Must EXACTLY match the "Valid OAuth Redirect URI" you register in the Meta
# Developer Dashboard (protocol + host + path, no trailing slash).
# Production (Vercel) example:
#   https://bot-sand-mu-74.vercel.app/integrations/instagram/callback
# For local testing this is typically:
#   http://localhost:5173/integrations/instagram/callback
VITE_INSTAGRAM_REDIRECT_URI=http://localhost:5173/integrations/instagram/callback

# AI backend (Render). Base URL only — no secrets.
VITE_AI_BACKEND_URL=https://botd-ai-backend.onrender.com

# --- Meta Embedded Signup (WhatsApp one-click connect) ---
# Public App ID from developers.facebook.com > Your App > Settings > Basic.
# Used only to initialise the Meta JavaScript SDK.
VITE_FACEBOOK_APP_ID=
# Facebook Login for Business > Configurations > "WhatsApp Embedded Signup".
VITE_FACEBOOK_CONFIG_ID=
